# meow LLM Detector 4.5.1 技术报告

[English](TECHNICAL_REPORT_EN.md) · [使用入口](README_CN.md) · [逐题数据与阈值](docs/EVIDENCE_CN.md)

本报告对应发行源码、GPT `4.5.0-rc4` 与 Claude `4.5.1-rc1`。应用版本和基准版本独立；保留基准原版本与摘要，不能为好看而重新命名实验记录。所有数值可在随包 JSON 中定位。本报告解释实现及其支持的结论，不把“通过用户验收”写成数学或安全证明。

## 1. 从问题到证据

目标是比较一个待测 API 的有限次回答与一组参考模型，而不是读取模型权重或确认供应商身份。链路为：

**固定请求 → 短答案 → 确定性归一 → 分窗计数 → 参考分布/漂移 → 多题似然 → 唯一越线 → 三色报告。**

[Bruckner 的论文](https://arxiv.org/abs/2607.10252)表明，简单短答的输出分布可以携带模型相关信息。论文报告完整40格电池约7.3%等错误率，8格低于约11%。本项目不是论文实验的复现：这里是四候选、五题、不同采样量和一个自定义组合判定器。下文同池模拟错指率与论文EER不是同一指标，不能用论文替本实现的准确率背书。

重要区分：请求名是用户发给服务商的字符串；基准候选是比较对象；报告的“强指向”只是这一候选集合中的分布判断。即使有OpenRouter来源，也没有获得模型权重的密码学认证。

## 2. 请求契约

内置题统一 `system="."`、`low`、无历史、无工具，最大输出128 token，流式响应。未显式设置temperature/top_p，供应商默认值因此仍是潜在变化源。低档每题4次、中档10次、高档20次，总计20/50/100；默认最多重试2次，每轮开始前显示请求上限。重复失败不算有效回答，重试不是额外统计样本。

| 模式 | API根地址后追加 | 关键载荷 | 请求外观 |
|---|---|---|---|
| GPT | `/responses` | input含system/user，reasoning.effort，store=false，max_output_tokens | 固定GPT/Codex UA |
| Claude | `/messages` | system、messages、thinking.type=adaptive、output_config.effort、max_tokens | claude-cli/2.1.251 (external, cli) |
| 其他 | `/chat/completions` | messages、reasoning_effort、流式usage、所选token上限字段 | 标准请求 |

完整UA和实际JSON见[请求示例](docs/REQUEST_EXAMPLES.json)，该文件从冻结契约和正式发送函数重建，**不是原始抓包**。认证头不包含在示例中。OpenRouter使用Bearer；Claude直连按发送器的Anthropic头处理。某供应商拒绝这些参数时会报告请求失败，不会偷偷去掉low仍宣称同契约。

推理强度不是输出长度或零思考保证。输出token上限可能同时约束可见答案和隐藏推理，参见[OpenAI官方说明](https://developers.openai.com/api/docs/guides/reasoning)。正式采样中偶尔较多推理没有被事后剔除。句点是否覆盖中转的隐藏提示词取决于上游实现，本程序无法保证；UA也不能证明服务端路由。

## 3. 基准内容与来源

一个不可变 `.meow.json` 包含：模式、候选ID与准确采集别名、每题/每格完整请求、normalizer及参数、family/group、分模型分窗观测、planned/completed质量字段、三档次数、拟合结果、阈值、模拟种子/批次/混淆表/契约摘要、采集来源和迁移说明、验证范围及内容摘要。实际字段以`benchmark.py`验证器为准，不允许运行包内代码或正则插件。

GPT五题为英文国家、英文鸟、B80字母计数、ZH027中文拟声词、ZH006中文国家。Claude五题为CL011造词选择、CL033字母选择、CL045整点选择、CL057座位选择、CL085地形选择。完整题面、答案类别和每对模型JSD由基准自动导出到[证据附录](docs/EVIDENCE_CN.md)，不在正文抄一份易过期的数据。B80有客观正确答案，是明确保留的特殊行为探针，不归入“无正确答案的任意选择题”。

2026-09-05正式采样：Claude2000条、GPT1100条，分别费用$0.565265/$0.4236336，零错误/重试；当时四窗整窗间隔至少15分钟。GPT当前包后来以400条新中文国家数据替换400条数字题数据，因此原整场费用不等于当前包所有观测的单独成本。

GPT旧三模型内置题保留历史分布：国家/鸟每模型120条、B80每模型80条。真实历史system为空，当前请求system为句点；这是维护者明确允许的迁移假设，不伪装成新采样。原GPT正式采集普通UA迁移到当前Codex UA也保留说明。中文新题没有套用这项历史豁免。

中文国家正式补池：Astra/Sol/Terra/Luna各100条，4窗×25，整窗间隔60.133/60.122/60.135秒，400条全部有效、无重试，$0.073706。之前两题64条小复核是选题证据，不混入正式池。未来生成器窗间隔统一至少60秒；短窗能发现短时间漂移，不能建立跨天稳定性。

默认包包含一条历史Sol英文国家INVALID观测，原计数保留。参考拟合和模拟排除它，报告质量字段仍可见。删除的是它参与概率计算的路径，不是把失败记录抹掉。

## 4. 归一化、有效样本与未知答案

答案先经过包中指定的确定性normalizer。英文行为标签去边缘标点、casefold并折叠空白；中文任意短答主要trim/casefold，不翻译、不猜同义词；B80只接受整数，再分为exact_3/other_integer。空白、越界长度或不合格式归为`__INVALID_OUTPUT__`。实际规则见`normalizers.py`。

合法但参考池从未出现的类别，在评分时归到`__OTHER__`。未知不等于无效，不能因为答案稀有就把它排除。反过来，HTTP200和流结束也不等于有效答案。

4.5.1新任务要求总体及每个启用格均至少 `ceil(0.6 × planned)` 份有效答案：逐格4次要求3份，10次要求6份，20次要求12份。任一格不足不能形成强指向；总体不足显示有效请求不足，0次代表该档禁用该格。旧任务保留90%规则，旧报告不重算。解析失败/无效答案与其他可重试失败共用有限预算，不无限补请求。原校准仍针对足额有效答案；部分样本未获得原99%准确率保证，门槛调整不是识别能力提升。

## 5. 参考分布、JSD与权重

对每一格，用所有候选的有效类别并集加OTHER形成同一个类别空间K。模型m的有效合计数为n，采用alpha=0.5加性平滑：

`p(m,k) = (n(m,k) + 0.5) / (Σk n(m,k) + 0.5K)`。

INVALID不进入类别空间或该分母。平滑防止零概率导致负无穷似然，但样本很少时也会明显影响JSD；不能不加说明地比较不同样本量/类别空间的JSD。

JSD使用以2为底的KL：`JSD(p,q) = 0.5 KL(p||(p+q)/2) + 0.5 KL(q||(p+q)/2)`，范围0至1。每格S是所有模型对的合并分布JSD均值。对每个模型，D_m为其非空窗口两两JSD的均值；D为已有D_m的均值。权重 `w=(S-D)/S` 当S>D，否则为0。缺少至少两窗时，漂移不可估计，推荐资格会区分，不把缺失当已证明稳定。

这不是用JSD直接判断一份测试。JSD用于描述参考差异和稳定性；实际判定使用下面的似然。S是均值，不能保证每个模型对都区分良好，因而选题还要看配对覆盖。

## 6. 多题评分和三色结论

待测格c有效计数为x，格似然是平均自然对数概率：

`L(c,m) = Σk x(c,k) ln p(c,m,k) / Σk x(c,k)`。

用平均而非总和，避免多采某题就线性放大其权力。增加采样主要减少该均值的随机波动。对于family f内的正权重格：

`F(f,m) = max(w_c) × Σc(w_c L(c,m)) / Σc w_c`；`score(m)=Σf F(f,m)`。

再做数值稳定softmax：`match(m)=exp(score(m)-max(score))/Σj exp(score(j)-max(score))`。这些数加起来为1，但**不是模型身份后验概率**，也不表示“98%请求是真模型”。它没有未知模型类的完整统计建模。

先检查有效样本、可用参考和校准绑定。随后计算越线集合`W={m:match(m)>threshold(m)}`：只有|W|=1才形成强指向；该唯一模型等于申报模型为绿，否则红。无模型越线或多个越线为黄。阈值因模型而异，所以唯一越线模型不一定是最高分模型，这是现行规则的明确含义；没有隐藏地改为argmax。显示值四舍五入，内部比较保留原精度。

family合并避免同一家族多格简单累加，但不是学习出的相关性模型。当前中英文国家分别是不同格/家族，题材相关；不能据此声称各题统计独立。模拟逐格抽样也不能证明对共同提示词偏移的抵抗力。

## 7. 校准算法和正确分母

当前scoring=`meow-fingerprint-v2`，simulation=`empirical-multinomial-pcg64-v5-valid-answers`。每个真模型、每个启用格按**有效原始计数比例**作multinomial抽样；拟合的平滑分布用于评分，抽样池不加平滑伪计数。每个模拟批次直接取得计划数量的有效答案。

默认每档1000万复查，四模型各250万，另各10万用于选线。PCG64随机种子为GPT49106/49107/49108，Claude47101/47102/47103。选线用SeedSequence[seed,0,model_index]，复查用[seed,1,model_index]；每块4096批。代码固定类别归约顺序，实际评分和批量模拟共用`numeric_matches`，避免BLAS批形差异改变严格边界。

选线覆盖q=.999：将自身分数升序排列，在`n-ceil(q*n)`处取边界，再取向负无穷方向相邻浮点数作为阈值，使边界样本能够严格越线。之后重放联合唯一越线规则，保存完整selection_confusion；不把“自己越线”直接当“唯一正确”。最后以不同随机子流复查，每个真模型的正确唯一指向占比都须≥.99才标target_met。并不自动降目标来获得合格。

**.999是选线覆盖，.99是复查目标；都不是模拟轮数、置信度或用户发起一次检测的成功率。** 假设某线路只有1%请求能产生有效答案，条件模拟仍可能很好看，但实际运行几乎无法过有效样本门槛。无效输出/网络故障的端到端影响没有被此指标回答。

千万批主要减少给定经验池的Monte Carlo误差，不会把每格100份真实观测变成千万份独立证据。选线与复查随机数独立，并不意味着有独立采样池。本版没有同契约独立四模型验证集，验证字段明确为false。

## 8. 当前结果与替换依据

| 组/档 | 最低条件正确指向率 | 错指批次/1000万 |
|---|---:|---:|
| GPT低 | 99.88276% | 43 |
| GPT中 | 99.89764% | 0 |
| GPT高 | 99.87628% | 0 |
| Claude低 | 99.88848% | 0 |
| Claude中 | 99.88344% | 0 |
| Claude高 | 99.89092% | 0 |

0表示没有观测到，不表示风险为零。各模型阈值、正确/错指/不足混淆表在证据附录和基准calibration.tiers中。

验收期间，待测数字题46/47比例偏离Astra正式池，但原因未证实。只删数字题的本地4万批/档诊断让低档Astra约95.65%、Sol98.43%，未达到目标，因此没有部署裸删的四题包。两题小复核后选择中文国家，完成四模型新池再替换。

新中文国家Astra主要巴西58/100、加拿大30/100；Sol冰岛86/100；Terra冰岛93/100；Luna加拿大92/100。Astra/Sol JSD .74061，四窗分别约.5859/.5395/.6870/.6369。Sol/Terra仅.05070，但英文国家和鸟题分别约.5725/.3557，提供其他区分线索。相关性和共同漂移仍是限制。旧rc3低档错指3872/1000万与新43/1000万是各自同池比较，不等于真实中转错指率下降了同样倍数。

## 9. 自定义生成器为什么这样选题

草稿先固定候选、准确API别名、协议、题面与归一器；可手填、批量导入或AI生成。AI每次最多10题，从12个宽领域/120主题中按种子抽关键词，保存seed/prompt版本和实际候选；只建议中性任意选择，不要求事实、最优解或理由。AI不知道区分度，也不生成参考分布或阈值。语义质量仍须人检查；重复和近似提示不是合规证明。

建议初筛每格每模型3份，再新增5份、后续新增8份；生成器每窗由用户确认，至少等1分钟。观察S、D、有效率及成本后手选或推荐。对模型对(a,b)，每格贡献：

`E(c,a,b)=w_c × min_model(valid_rate) × max(0,JSD(a,b)-max(Dmax_a,Dmax_b))`。

只考虑当前低档启用格和可估漂移的候选，同一题多格取max。锁定先入、同源组最多一题。贪心按从低到高排序的配对覆盖向量作字典序改善；平局按请求数、已知费用中位数、ID。不用复杂优化器，不为凑题数强加零贡献题。界面展示理由、最弱模型对及排除原因。

相似题提示用词元集合Jaccard≥.90，仅展示前100对和总数，不自动删除。推荐对照使用同一有效池、同格共享抽样、每模型2000批、seed45006，比较唯一最高分率，而非强指向通过率。平均JSD比较器遵守相同资格/锁定/同源组和推荐的实际预算/题数上限；成本不等时直接标明，不假装公平。该预览不改正式评分。

题目/请求/候选别名变更后不能继续混用旧窗；名称/三档数量改动可以保留采样，但次数变化须重校准。模拟任务冻结输入、保存checkpoint，可暂停/恢复/导出；切换网页草稿不改变运行任务归属。

## 10. 运行、安全与可追溯性

运行结构是本地HTTP界面、共享有限执行器、HTTPX传输、SQLite单writer事务及NumPy评分，无服务端框架/ORM/Node运行依赖。报告显示两种API根地址，历史报告取自身冻结endpoint，不取当前输入框。

模型请求默认HTTPS，回环允许HTTP，非回环明文要明确开启；拒绝userinfo/query/fragment，不跟随模型请求重定向。OpenRouter精确HTTPS来源共享最多4在途和响应头限流；显式443与省略443共用gate。Limit20不推断为每分钟20次。环境/Windows手动代理共用；不执行PAC。

服务仅绑定127.0.0.1，校验Host/Origin与会话令牌，静态资产固定表，数据目录持有OS锁。它不是多用户身份边界，不应公开部署。JSON有大小/类型限制，TLS、有限重试、取消和deadline不因“精简”删除。SQLite保留冻结任务与累计attempt，排队取消不算派发，已发取消仍计额度；恢复不会清零账单。

临时key只保留于页面和活动后台引用；无localStorage。后台断开/会话过期清页面密码，结束每轮释放后台引用。显式保存连接才进入系统凭据库；定时计划持有独立凭据引用，改预设不会静默改计划。每个定时检测单独出报告，不滚动叠加证据。

可选留存保存逻辑请求JSON、解码后UTF-8响应、选定响应头、状态和完整性标志，并与attempt/result同事务。不是TLS线缆字节，不保存认证头。遇到实际凭据回显，拒绝留存正文；敏感字段/常见key格式还会脱敏，但不能保证识别所有编码变体。未经人工检查的用户报告不自动公开。

这次公开的是冻结请求、分窗归一计数和校准结果，不打包私人SQLite/完整原始响应。本版未发布原始网络抓包，不能把重建请求示例称为真实响应证据。v4.0历史原始材料链接仍可从原发行页追溯，但不是4.5.0独立验证。

## 11. 校验、目录与复算

content_sha256基于规范化JSON语义；目录另有下载字节sha256。前者检测内容一致，后者检测文件传输，两者都不是作者签名。维护者身份来自发行清单/维护的目录，来源URL徽标只说明采集出处。新建和恢复检测均检查当前缓存的安全撤回；不强制联网，但离线就无法知道刚发布的撤回。

`benchmarks/index.json`包含官方包及可经PR提交的社区包。投稿校验不执行包内容、不读取仓库秘密，不自动背书或自动合并。全新模型也可能超出当前候选集合，社区来源与数值校准资格分开。

安装依赖后在仓库根目录运行：

```sh
python scripts/reproduce.py --check-only
python scripts/reproduce.py --mode gpt --quick
python scripts/reproduce.py --mode gpt
python scripts/reproduce.py --mode claude
python -m unittest discover -s tests -v
node tests/report_urls.cjs
python scripts/build_release.py --output dist
```

check-only只验包；quick为8000批/档自检，不冒充发布结果；默认完整复算三档各1000万，沿用包内种子/覆盖/目标，不发网络请求。输出独立目录，比较阈值和混淆表，不修改官方包。运行库版本见`docs/REPRODUCTION_ENV.json`；严格浮点边界可能随Python/NumPy/平台变化，应报告差异而不是重签旧结果。构建脚本固定文件顺序/ZIP时间戳，双语只换默认语言和README入口，基准字节不变。

## 12. 仍不能回答的问题

独立新池验证、跨天/账号/IP稳定性、注入提示/量化/推理设置的系统实验、未知候选拒识和恶意固定题路由未由本版解决。论文、用户验收、子代理复核、单元测试和同池模拟分别回答不同问题，不能相互替代。合理使用方式是把报告当成可重复的异常线索，再结合请求ID、时间、服务商记录和独立复测，而不是单凭颜色定案。
