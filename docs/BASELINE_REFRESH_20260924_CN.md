# 2026-09-24 新模型基准

本次仅更新可下载基准，不发布新程序版本，也不重算历史报告。在检测器的基准页面检查更新即可安装。

- GPT：更新 Sol、Luna 为第六代，保留 Astra 和 Terra；旧 Sol、旧 Luna 纳入 `other`。三题，快速/中档/高档分别32/64/128次，推荐中档。
- Claude：更新 Opus 为5.5，保留 Fable5.1、Sonnet5、Haiku4.5；旧 Opus5纳入 `other`。四题，48/72/120次。
- 评分公式、60%有效样本资格、98%强指向线上限不变；不设置温度。

## 验证结果与边界

新采集1520份全部成功，其中1140份用于拟合、380份预先封存用于检验。当前候选和本次采集的退役型号每题用30份开发答案拟合，10份验证答案不参与拟合。其他参考来源保留匹配的历史数据。

每来源10000条经验重采样路径中，GPT当前候选最低满额正确强指向为97.21%/99.40%/99.94%；Claude当前候选三档均为100%。模拟并不等于10000次真实独立检测，更不保证现实零错误。GPT快速档真Sol证据不足2.67%；中档other线从50%微调至51%后，真Sol满额错误由0.60%降至0.13%，证据不足0.47%。

本次验收针对常见中转替换。旧Luna误指新Luna不作为业务否决项，但仍按错误原样记录；没有声称能识别所有未知模型。初始方案先冻结再开封验证，业务验收口径及中档微调在检验后确定，所以最终方案不称为全程独立盲测通过。

旧检测器的通用校准字段只能表达所有来源共同下限。GPT快速档为兼容而明确采用全来源70%正确/12%任一路径错误的包络，包含旧Luna72.26%正确、2.90%错误、24.84%不足；这不是当前候选的质量目标。包内另有绑定的业务验收指标：快速当前候选97%、中高99%，排除指定方向后早段/全路径3%、后段/满额1%。原始计数不修改，不把例外伪装成正确。

检测不符不等于中转站故意替换模型，也可能与路由变化、服务降级或基准局限有关。
