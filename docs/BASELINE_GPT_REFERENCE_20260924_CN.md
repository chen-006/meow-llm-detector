# GPT 参考范围更新（20260924.2）

原生 GPT 基准升级为 `4.5.4-predictive.20260924.2`，GPT Chat 兼容版升级为 `4.5.4-chat.20260924.2`，均高于此前 `.1`。程序和 Claude 基准不变。

按维护者决定移除 `other` 中 GPT-5.5 的参考。保留旧 Sol、旧 Luna、GLM；题目、32/64/128请求分配、其他来源的原始观测、强指向线、60%资格和评分算法不变。按现有算法重建类别词表及平滑分布。新版不再专门保证识别 GPT-5.5。

使用原有封存样本进行每来源10000条经验重采样，重新检查完整检测、20%缺样本、延迟题目及随机顺序的合格前缀。验证包含所有保留来源，仍如实保留旧 Luna→新 Luna 的原始混淆。没有新增付费采样，未将封存样本用于拟合。

这不是新独立验证或真实线上准确率证明，不能承诺已经解决所有可信中转的 Sol 误判。Chat 版沿用原生采集与验证，保留协议派生来源。旧包与历史报告不修改。
